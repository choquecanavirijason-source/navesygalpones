/**
 * Modelo de referencia de la feature `example`.
 * Plantilla para copiar al crear una feature real: reemplazar por la forma
 * definitiva de los datos que devuelve el backend.
 */
export interface ExampleItem {
  id: string;
  name: string;
}
